{% extends 'base.html.twig' %}

{% block title %}Team{% endblock %}

{% block body %}
    <h1>Team</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ team.id }}</td>
            </tr>
            <tr>
                <th>Nom d'equipe</th>
                <td>{{ team.name }}</td>
            </tr>
             <tr>
                <th>Noms des emploiyeurs</th>
                <td> {% for user in team.users %}
				 {{  user  }} <br>
				 {% endfor %}</td>
            {# </tr>
               <tr>
                <th>Noms des emploiyeurs</th>
                <td> {% for prospect in team.prospects %}
				 {{  prospect  }} <br>
				 {% endfor %}</td>
            </tr> #}
            <tr>
                <th>Description</th>
                <td>{{ team.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_team_index') }}">back to list</a>

    <a href="{{ path('app_team_edit', {'id': team.id}) }}">edit</a>

    {{ include('team/_delete_form.html.twig') }}
{% endblock %}
